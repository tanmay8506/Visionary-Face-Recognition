[build]
builder = "nixpacks"
buildCommand = "cd server && npm install"

[deploy]
startCommand = "cd server && node migrate.js ; node index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
